services:
  seiyuu:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://seiyuu.page}
        NEXT_PUBLIC_ICP_BEIAN_NUMBER: ${NEXT_PUBLIC_ICP_BEIAN_NUMBER:-}
        NEXT_PUBLIC_MPS_BEIAN_NUMBER: ${NEXT_PUBLIC_MPS_BEIAN_NUMBER:-}
    container_name: seiyuu
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL:-https://seiyuu.page}
